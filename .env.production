# Vercel Production Build Configuration

# Set to false to prevent ESLint warnings from failing the build
# (they'll still show as warnings in the logs)
SKIP_PREFLIGHT_CHECK=true

# Disable type checking during build to speed up Vercel deployments
DISABLE_ESLINT_PLUGIN=false

# React mode
REACT_APP_ENV=production
